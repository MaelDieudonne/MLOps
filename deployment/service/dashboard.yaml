apiVersion: v1
kind: Service
metadata:
  name: movie-reviews-dashboard-service
spec:
  selector:
    app: movie-reviews-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
