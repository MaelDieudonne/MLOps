apiVersion: v1
kind: Service
metadata:
  name: movie-reviews-api-service
spec:
  selector:
    app: movie-reviews-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8001
  type: LoadBalancer
